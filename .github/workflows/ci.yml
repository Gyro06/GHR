name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Show files
        run: |
          echo "Listing repository files"
          ls -la
      - name: Sanity check
        run: |
          echo "CI run for GHR"
